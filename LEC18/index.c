#include <stdio.h>
int main()
{
    //Typecasting 
    // (Type) value;
    int a =3;
    float b =54/5; //int to int caculation then result will be int
    //but if we want int to float or vice versa 
    printf("The value of b is %d\n", (int) b);
    return 0;
}